{
  "name": "deliver",
  "package": "pi-shipyard",
  "description": "Focused delivery: understand, plan, implement with one writer, two-angle review, verified fixes, and final readiness validation.",
  "chain": [
    {
      "agent": "pi-shipyard.codebase-reader",
      "phase": "Understand",
      "label": "Build delivery context",
      "as": "scope",
      "task": "Build an implementation brief for this explicitly approved delivery task:\n\n{task}\n\nRead repository instructions, current behavior, source/callers/tests/config/docs, and git status. Define constraints, non-goals, affected paths, seams, risk map, and validation harness. Do not edit project/source files.",
      "output": "scope/delivery-brief.md",
      "outputMode": "file-only",
      "progress": false,
      "acceptance": {
        "level": "none",
        "reason": "Read-only analysis or validation; only configured artifacts and the run-scoped findings ledger may be written."
      },
      "toolBudget": {
        "soft": 18,
        "hard": 30,
        "block": "*"
      }
    },
    {
      "agent": "pi-shipyard.delivery-planner",
      "phase": "Plan",
      "label": "Create implementation plan",
      "as": "plan",
      "task": "Create an implementation-ready plan for: {task}\n\nScope artifact: {outputs.scope}\n\nOpen and read the referenced scope file. Verify critical claims against source. Define ordered changes, seams, validation contract, risks, non-goals, and escalation points. Do not edit project/source files.\n\nOpen and read every referenced output artifact before reasoning; file-only references contain paths, not artifact contents.",
      "output": "plan/implementation-plan.md",
      "outputMode": "file-only",
      "progress": false,
      "acceptance": {
        "level": "none",
        "reason": "Read-only analysis or validation; only configured artifacts and the run-scoped findings ledger may be written."
      },
      "toolBudget": {
        "soft": 18,
        "hard": 28,
        "block": "*"
      }
    },
    {
      "agent": "pi-shipyard.implementation-worker",
      "phase": "Implement",
      "label": "Implement approved task",
      "as": "implementation",
      "task": "Implement the approved task as the sole active-worktree writer.\n\nTask: {task}\nScope artifact: {outputs.scope}\nPlan artifact: {outputs.plan}\nFindings store reserved for later review: {{SHIPYARD_STORE}}\n\nOpen and read both referenced files. Implement only approved scope, add focused tests, run the validation contract, inspect the diff, and return the required implementation handoff. Decide reversible product/API/architecture/migration/security questions yourself with the conservative option that best matches existing patterns, and record each in a Decisions made section of the handoff; escalate only before irreversible or destructive actions. Do not commit or push.\n\nOpen and read every referenced output artifact before reasoning; file-only references contain paths, not artifact contents.",
      "output": "implementation/initial-handoff.md",
      "outputMode": "file-only",
      "progress": true,
      "acceptance": "checked"
    },
    {
      "phase": "Independent review",
      "parallel": [
        {
          "agent": "pi-shipyard.contract-reviewer",
          "label": "Contract and integration review",
          "as": "contracts",
          "task": "Review the implementation for requirement, invariant, API, compatibility, persistence, configuration, packaging, and integration defects.\n\nTask: {task}\nScope: {outputs.scope}\nPlan: {outputs.plan}\nImplementation handoff: {outputs.implementation}\nFindings store: {{SHIPYARD_STORE}}\n\nOpen all referenced artifacts and inspect the actual diff/source/tests plus affected callers. Record concrete findings using stage `wave-1-contracts` and sourceRole `pi-shipyard.contract-reviewer`. Stay independent and do not edit project/source files.\n\nIndependent-wave rule: do not call review_findings list, get, stats, snapshot, or update; add proposals directly and let the next stage adjudicate.\n\nOpen and read every referenced output artifact before reasoning; file-only references contain paths, not artifact contents.",
          "output": "review/wave-1-contracts.md",
          "outputMode": "file-only",
          "progress": false,
          "acceptance": {
            "level": "none",
            "reason": "Read-only analysis or validation; only configured artifacts and the run-scoped findings ledger may be written."
          },
          "toolBudget": {
            "soft": 20,
            "hard": 32,
            "block": "*"
          }
        },
        {
          "agent": "pi-shipyard.adversarial-tester",
          "label": "Runtime and adversarial review",
          "as": "adversarial",
          "task": "Trace runtime, state, error, cleanup, retry, and concurrency behavior, then execute focused counterexamples against the implementation.\n\nTask: {task}\nScope: {outputs.scope}\nPlan: {outputs.plan}\nImplementation handoff: {outputs.implementation}\nFindings store: {{SHIPYARD_STORE}}\n\nOpen all referenced artifacts and inspect the actual diff/source/tests. Record concrete findings using stage `wave-1-adversarial` and sourceRole `pi-shipyard.adversarial-tester`. Stay independent and do not edit project/source files.\n\nIndependent-wave rule: do not call review_findings list, get, stats, snapshot, or update; add proposals directly and let the next stage adjudicate.\n\nOpen and read every referenced output artifact before reasoning; file-only references contain paths, not artifact contents.",
          "output": "review/wave-1-adversarial.md",
          "outputMode": "file-only",
          "progress": false,
          "acceptance": {
            "level": "none",
            "reason": "Read-only analysis or validation; only configured artifacts and the run-scoped findings ledger may be written."
          },
          "toolBudget": {
            "soft": 20,
            "hard": 32,
            "block": "*"
          }
        }
      ],
      "concurrency": 2,
      "failFast": false
    },
    {
      "agent": "pi-shipyard.falsifier",
      "phase": "Review decision",
      "label": "Verify review findings",
      "as": "falsification",
      "task": "Adjudicate the independent review findings for: {task}\nScope: {outputs.scope}\nPlan: {outputs.plan}\nImplementation: {outputs.implementation}\nReceipts: {outputs.contracts} {outputs.adversarial}\nFindings store: {{SHIPYARD_STORE}}\n\nOpen all referenced artifacts. Snapshot the store with stage label `wave-1`, then reproduce, reject, or narrow every proposal using optimistic revisions. Produce the compact set of verified fixes worth doing now and their validation requirements. Do not edit project/source files.\n\nOpen and read every referenced output artifact before reasoning; file-only references contain paths, not artifact contents.",
      "output": "review/verified-fixes.md",
      "outputMode": "file-only",
      "progress": false,
      "acceptance": {
        "level": "none",
        "reason": "Read-only analysis or validation; only configured artifacts and the run-scoped findings ledger may be written."
      },
      "toolBudget": {
        "soft": 24,
        "hard": 40,
        "block": "*"
      }
    },
    {
      "agent": "pi-shipyard.implementation-worker",
      "phase": "Fix",
      "label": "Apply verified fixes",
      "as": "fixes",
      "task": "Apply verified review fixes as the sole active-worktree writer.\n\nTask: {task}\nScope: {outputs.scope}\nPlan: {outputs.plan}\nInitial implementation: {outputs.implementation}\nVerified review: {outputs.falsification}\nFindings store: {{SHIPYARD_STORE}}\n\nOpen all referenced artifacts and ledger records. Apply only verified fixes worth doing now, update resolved findings with expected revisions after validation, run focused checks, inspect the diff, and return the required fix handoff. Do not commit or push.\n\nOpen and read every referenced output artifact before reasoning; file-only references contain paths, not artifact contents.",
      "output": "implementation/fix-handoff.md",
      "outputMode": "file-only",
      "progress": true,
      "acceptance": "checked"
    },
    {
      "agent": "pi-shipyard.shipwright",
      "phase": "Delivery readiness",
      "label": "Validate and prepare final handoff",
      "as": "final",
      "task": "Prepare the final delivery-readiness handoff for: {task}\nScope: {outputs.scope}\nPlan: {outputs.plan}\nInitial implementation: {outputs.implementation}\nVerified review: {outputs.falsification}\nFix handoff: {outputs.fixes}\nFindings store: {{SHIPYARD_STORE}}\n\nOpen every referenced artifact and inspect the final diff/status. Directly verify resolved findings and rerun the complete validation contract against the final worktree after fixes; never rely on pre-fix or worker-reported results. Any required check that fails or cannot run yields NOT READY. Record any distinct final defect using creation stage `final-validation` and sourceRole `pi-shipyard.shipwright`. Export the ledger to {{SHIPYARD_RUN_DIR}}/findings.md. Return READY TO SHIP, NOT READY, or NEEDS DECISION with evidence, residual risks, and exact next commands. When the change has user-visible behavior, include a Behavior check section. Never commit or push.\n\nOpen and read every referenced output artifact before reasoning; file-only references contain paths, not artifact contents.",
      "output": "delivery/final-handoff.md",
      "outputMode": "inline",
      "progress": false,
      "acceptance": {
        "level": "none",
        "reason": "Read-only analysis or validation; only configured artifacts and the run-scoped findings ledger may be written."
      }
    }
  ]
}
