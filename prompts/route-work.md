---
description: Route a task through Pi Workbench's smallest suitable execution mode
argument-hint: "<task>"
---

Use the `pi-workbench` skill and `workbench_route` to choose the smallest suitable mode for this task. Do not nest orchestration modes. Prefer a one-off role unless fixed lifecycle gates, peer communication, or bounded programmatic branching is genuinely required.

Task:

$@
