---
description: Create an implementation-ready plan from direct codebase evidence
argument-hint: "<feature, fix, or change>"
---

Use a fresh `pi-shipyard.codebase-reader` pass followed by `pi-shipyard.delivery-planner` for:

$@

Keep the handoff file-backed with named outputs and `outputMode: "file-only"`. The final plan must define approved behavior, non-goals, files/seams, ordered one-writer steps, validation contract, risks, and user-owned decisions. Do not edit project/source files.
